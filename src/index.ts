export const starter = '# Welcome to Your New TypeScript Project!'

/**
 * Greets a person with their name.
 * @param name - The name of the person to greet.
 * @return A greeting message.
 */
export function greet(name: string): string {
  return `Hello, ${name}!`
}
